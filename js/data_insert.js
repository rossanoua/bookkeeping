$( document ).ready( function(){
    
    //this will fire once the page has been fully loaded
    
    $( '#data-post-btn' ).click( function(){
        console.log( "button is working just fine" );
    });
    
});
